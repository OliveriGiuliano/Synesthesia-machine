# Architecture

`master.md` is the active architectural baseline. Binding deviations and later decisions are recorded
as accepted ADRs under `docs/adr`.

The numbered delivery roadmap retained in the master document explains how v0 was built; it is
non-normative. Current implementation ownership follows the package and domain boundaries described
in `AGENTS.md` and the playbooks under `docs/agent-playbooks`.
