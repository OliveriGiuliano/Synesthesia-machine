# Benchmark fixtures

This directory owns deterministic, non-user inputs for performance harnesses. They are separated from
`examples` because their topology, media placeholders, and dimensions serve a measurement contract
rather than a user tutorial.

Run benchmark commands through `uv` and read `tools/README.md` before interpreting their output.
