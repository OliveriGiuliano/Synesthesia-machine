# Packaging skeleton

Phase 0 reserves this directory and records environment reports. Production standalone deployment
configuration belongs to Phase 9 after runtime boundaries are stable.

The first release packaging route is `pyside6-deploy` in standalone mode, not one-file mode.